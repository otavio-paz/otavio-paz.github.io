---
layout: default
---
{% if page._styles %}
  <!-- Page/Post style -->
  <style type="text/css">
    {{ page._styles }}
  </style>
{% endif %}

<div class="post{% if page.permalink == '/projects/' %} interior-page projects-page{% endif %}">
  <header class="post-header">
    {% unless page.permalink == '/projects/' %}
      <h1 class="post-title">{{ page.title }}</h1>
      {% unless page.hide_description %}
        <p class="post-description">{{ page.description }}</p>
      {% endunless %}
    {% endunless %}
  </header>

  <article>
    {{ content }}
  </article>

  {% if page.related_publications %}
    <h2>References</h2>
    <div class="publications">
      {% bibliography --cited_in_order %}
    </div>
  {% endif %}

  {% if site.giscus and page.giscus_comments %}
    {% include giscus.liquid %}
  {% endif %}
</div>
